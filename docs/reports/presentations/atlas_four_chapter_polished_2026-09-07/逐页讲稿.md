> Markdown 归档副本，整理于 2026-09-23。原报告与媒体：[原始文档](../../../../presentations/atlas_four_chapter_polished_2026-09-07/逐页讲稿.md)。本目录未复制视频或大型资产。

# Atlas：四章递进讲稿

主讲 26 页，附录 8 页。

顺序：总体架构 → 功能拆解 → 开源替代与复现路线 → 本地 WorldWarp。

原系统图在第 2 页；原开源组装图在第 10 页；本地四段视频在第 24 页。

## 01｜Atlas：从总体架构到本地实证

开场句
这次按四个层次讲：先解释我认为系统是什么，再解释各项功能，然后选开源部件，最后展示本地 WorldWarp。

展开讲
1. 第一章只建立总架构；第二章暂时不讨论项目部署，专注模块的输入、输出与反馈关系。
2. 第三章才把功能映射到开源项目，并给出复现顺序和验收。第四章集中讲 WorldWarp 的本地环境、使用方式、四段真实结果以及仍需补的接口。
3. 系统原图与开源组装原图完整保留，分别在第 2 页和第 10 页。

过渡句
先从我的整体架构图开始。

### 来源 / 设计依据

- [系统综合图 v2](../../../../diagrams/atlas_architecture_synthesis_v2.svg)
- [原开源组装架构图](../../../../diagrams/atlas_open_source_assembly.svg)
- [WorldWarp 教室四段实测](../../../../WorldWarp_outputs/classroom_pan_left_2026-09-07/README.md)

## 02｜总体架构：统一世界模型与 GS 时空闭环

开场句
这就是我的完整系统图。先看区域与箭头，不要求在这一页逐字读完。

展开讲
1. 上层是多模态输入、Marble 式初始化以及空间上下文。初始 GS 进入左下方的持久世界状态，参考图和历史进入上方的上下文。
2. 中层从已有世界在目标相机渲染粗图，统一 AR 扩散世界模型结合参考产生新 RGB-D，再做三维增量融合，写回同一世界；新 RGB-D 同时更新历史。
3. 下层是 4D：固定时刻换相机、固定相机推进时间，两种操作共享静态背景和动态状态。底部统一输出图像、视频、点云 / GS 与动态观测。
4. 原图在本页以完整矢量图嵌入；没有裁掉初始化、空间上下文、动态分支或输出。接下来几页用大字版按这些区域展开。

过渡句
先把这张总图读成一个完整系统，再进入具体功能。

### 来源 / 设计依据

- [系统综合图 v2](../../../../diagrams/atlas_architecture_synthesis_v2.svg)

## 03｜用一张图串起初始化、空间闭环与 4D

开场句
我的假设是：同一个世界模型负责产生内容，GS 保存世界，空间上下文保存看过的参考。

展开讲
1. 多模态输入先经过初始化，得到局部 GS 和参考图。初始化与连续生成可理解为同一统一模型的不同使用阶段。
2. 一轮空间探索从当前世界选 C、t 并渲染；粗图和空间上下文进入统一模型，生成新视图及深度；增量融合把它们变成更新后的世界。
3. 上下文与世界状态同时反馈。这里生成与深度仍保留为统一模型假设，不提前按开源项目拆成两台模型。
4. 4D 不是最后被删掉的一小项，而是整个系统的另一类查询：固定时间探索空间，或固定相机推进时间。具体长期 GS 写回与模块拓扑是原综合假设，不当作官方未披露算法。

过渡句
下面进入第二章，依次解释初始化、渲染、生成、融合、记忆与 4D。

### 来源 / 设计依据

- [系统综合图 v2](../../../../diagrams/atlas_architecture_synthesis_v2.svg)

## 04｜功能一：多模态初始化与参考建立

开场句
我的初始化并不只是生成一张漂亮图片，而是留下后续能继续使用的三维区域和参考。

展开讲
1. 单图、多图直接提供参考；纯文本先得到视觉参考，再结合深度和相机形成点云、GS。视频、多视角视频或已知几何也是原图保留的入口。
2. Marble 式表示输入到局部三维区域的能力。假设层可以由统一模型承担参考与深度生成，再转换成 GS；不是要求必须调用 Marble 产品。
3. 一份输出是可以渲染的初始 GS，另一份是首帧参考、首帧 GS 渲染及其相机等上下文。观测图可以作为 GT；由文本生成的首图应称参考锚点。
4. 此时空间世界与参考记忆已经有起点，之后不必每轮从头建立一个完全独立场景。

过渡句
建立初始世界后，先在目标相机下渲染已有内容。

### 来源 / 设计依据

- [系统综合图 v2](../../../../diagrams/atlas_architecture_synthesis_v2.svg)

## 05｜功能二：从已有世界渲染目标视角

开场句
渲染先把目标相机位置与已有三维世界联系起来，给生成模块一份可利用的空间约束。

展开讲
1. 输入是当前世界状态、目标相机与时刻。静态探索先固定 t，只小幅改变 C。
2. 输出一类是粗 RGB，另一类是深度与几何可见性 / 有效性信息。已有区域可以约束新图像，遮挡后新暴露处可能缺内容。
3. 这一页不绑定某个渲染库。第三章再说明 gsplat、GaME 渲染器和 WorldWarp 内部渲染的实现选择。

过渡句
有了粗视图，接下来按我的三组图像条件生成完整的新视角。

### 来源 / 设计依据

- [系统综合图 v2](../../../../diagrams/atlas_architecture_synthesis_v2.svg)

## 06｜功能三：几何引导的新视角生成

开场句
我先用已有 GS 渲染稍微变化后的相机视角：大部分内容还在，但新暴露处可能有洞或粗糙。

展开讲
1. 第一组输入是当前目标视角的粗 GS 渲染，可以带深度与可见性；第二组是首帧参考 RGB；第三组是首帧 GS 渲染。三组图像条件在这个版本里都明确保留。
2. 首帧参考约束外观，首帧 GS 渲染提供参考与三维表达之间的对应，目标粗渲染提供当前相机的结构。历史 RGB-D、相机、时间和文本一并进入上下文。
3. 统一世界模型按时序 / AR 组织生成，在空间条件引导下修整已有内容并补出未知区域，得到新 RGB 和深度。用户最初描述的新 RGB 帧，在综合假设图中保留了统一 RGB-D 输出能力。
4. 稍后开源实现会把 RGB 生成和几何预测拆开；这里先完整讲原假设，不提前替换为 WorldWarp + MapAnything。

过渡句
生成出的新帧还需要写回到三维世界。

### 来源 / 设计依据

- [系统综合图 v2](../../../../diagrams/atlas_architecture_synthesis_v2.svg)

## 07｜功能四：几何回写与增量融合

开场句
我要求的不只是看到新画面，而是新画面里的新区域要进入同一份三维世界。

展开讲
1. 新 RGB-D 与相机产生候选点云；类似 SLAM 建图的一侧负责对齐旧地图、融合和优化 GS。这里说的是功能，不假设某个开源项目已经完整提供跟踪与回环。
2. 通过检查的新区域写回世界，下一轮从更新的 GS 渲染，所以刚刚探索的内容成为之后的几何约束。
3. 同时，新 RGB-D 和相机经过选择进入空间上下文，下一轮生成仍能看到这部分历史。两条反馈是原架构的关键。
4. 在静态探索时固定 t；动态情况写入对应的动态层或时刻状态，不能把不同时间的运动都不加区别地融进静态 GS。

过渡句
这里实际上有两种记忆，下一页把它们的职责分清。

### 来源 / 设计依据

- [系统综合图 v2](../../../../diagrams/atlas_architecture_synthesis_v2.svg)

## 08｜功能五：世界地图与空间上下文

开场句
原图中这两份记忆，分别解决空间定位与参考一致性。

展开讲
1. 世界状态保存 GS、几何与可见性，供目标相机渲染；参考记忆保存首帧参考、首帧 GS 渲染和历史 RGB-D / 相机，供生成使用。
2. 两路共享坐标与时间标识。新增历史要筛选，不表示把所有帧无限放进模型上下文。

过渡句
静态闭环的功能清楚后，再把相机与时间这两类控制放进去。

### 来源 / 设计依据

- [系统综合图 v2](../../../../diagrams/atlas_architecture_synthesis_v2.svg)
- [World Labs · Atlas](https://www.worldlabs.ai/blog/atlas)

## 09｜功能六：相机与时间的独立控制

开场句
我对 4D 的设想是先把操作分为两种：同一时刻换视角，或者同一视角看时间变化。

展开讲
1. 空间路径固定 t、改变 C：查询该时刻的静态背景和动态状态，渲染、生成并按需补充空间几何，复用前面的闭环。
2. 时间路径固定 C、推进 t：统一模型结合历史生成或重构下一时刻的 RGB-D，把变化写入动态 4DGS 或形变表示，再渲染新时刻。
3. 因此世界不是只有一份静态 GS，而是静态背景加动态层。C 和 t 也可以同时变化。完整模型仍要处理遮挡、多视角与时序一致性。
4. 这是一种可实现的控制 / 调度假设，不需要先声称底层网络完全解耦。实施时可以先验收静态闭环，再接动态；但动态分支在我的总体架构中一直保留。

过渡句
系统功能讲完了，第三章开始讨论开源项目如何分别承接这些职责。

### 来源 / 设计依据

- [系统综合图 v2](../../../../diagrams/atlas_architecture_synthesis_v2.svg)

## 10｜开源组装架构：各项目如何承接系统功能

开场句
这是系统假设对应的原开源组装图。原来选过的初始化、记忆、生成、融合和动态部件都在。

展开讲
1. 上层用 HY-World 2.0 对应初始化，Spatia 的参考检索和历史机制对应上下文。中层用 gsplat 渲染，Spatia 加 MapAnything 提供 RGB-D，GaME 加掩码适配承担三维融合。
2. 下层保留 NoPo4D / ClipGStream 等动态重建候选，以及 LongLive 等时间后端备选；WorldWarp 和 AlayaWorld 是替换生成主干的候选，不要求全部串联。
3. 图里橙色本来就标出了需要自写的 Session、GS 条件适配、初始资产导入、同步和流式接入。展示原图不表示这些项目现已全部安装联调。
4. 本地先用已运行的 WorldWarp 做生成基线，只是实施选择；它没有替代或删除我的原组装设计。原图的日期和版本标签不在本轮重新研究或改写。

过渡句
把这张开源图按功能拆成对应表，方便比较与替换。

### 来源 / 设计依据

- [原开源组装架构图](../../../../diagrams/atlas_open_source_assembly.svg)
- [既有开源组装研究](../../archive/ATLAS_OPEN_SOURCE_ASSEMBLY_2026-09-06.md)

## 11｜从功能到项目：明确每个候选的替代范围

开场句
这页只做精确对应，不把系统图缩成仅剩三个项目。

展开讲
1. 初始化、空间上下文、渲染、RGB-D 生成、三维融合与动态表示分别对应不同职责；其中有些项目负责多个职责，有些职责需要项目加自写代码组合。
2. 原图中的统一 RGB-D 模型在开源实现里可以拆成 RGB 生成器与几何预测器，拆分的是实现，不是把原假设中的深度输出删除。
3. 原方案保留 Spatia 等候选，本地基线可以采用 WorldWarp。gsplat、GaME 自带渲染器与 WorldWarp 的内部渲染不能默认视为已相互替换，需要相机与条件适配。
4. 完整候选、日期和外围工具仍可回到上一页原图查看。本地只验证了 WorldWarp 与几何主链，不能把这一对应表说成全部安装完成。

过渡句
先解释实现层如何拆分统一模型，再讨论具体项目。

### 来源 / 设计依据

- [系统综合图 v2](../../../../diagrams/atlas_architecture_synthesis_v2.svg)
- [原开源组装架构图](../../../../diagrams/atlas_open_source_assembly.svg)
- [既有开源组装研究](../../archive/ATLAS_OPEN_SOURCE_ASSEMBLY_2026-09-06.md)

## 12｜实现策略：统一模型的功能，可以分模块复现

开场句
我的系统假设保留统一 RGB-D 模型；开源复现可以先按功能拆开。

展开讲
1. 统一模型承担生成与几何表达，在开源实现中可用 Spatia 或 WorldWarp 生成 RGB，再用 MapAnything 预测几何，交给 GaME 写入场景。
2. 这是工程分解，模型结构和误差传播会不同，但原架构中的输入、空间上下文、世界状态和反馈关系都应保留。

过渡句
先用输入输出表把三个主要候选的角色分清。

### 来源 / 设计依据

- [World Labs · Atlas](https://www.worldlabs.ai/blog/atlas)
- [既有开源组装研究](../../archive/ATLAS_OPEN_SOURCE_ASSEMBLY_2026-09-06.md)
- [本地几何链路验收](../../archive/GEOMETRY_SETUP.md)

## 13｜三个主要候选：输入、输出与核心职责

开场句
这三行对应三个不同问题：怎么生成新内容，怎么恢复几何，怎么维护可渲染的场景。

展开讲
1. WorldWarp 的直接演示入口是一张起始图加相机运动，可输出一段新视角视频，并留下用于生成条件的局部高斯缓存；后续段可以读取上一段视频。
2. MapAnything 可接一张或多张图。多视角输入最好属于同一场景、有重叠且时刻一致，输出深度、相机内外参、pointmap 等；本地点云 PLY 由包装器聚合导出。
3. GaME 接带相机参数的 RGB-D；完整场景变化处理还需要掩码。它优化的是当前场景的 Gaussians，既能保存资产也能保存场景状态。
4. 这三个项目不是三种同类型的视频模型，也不是三种可以无条件互换的 3D 模型。

过渡句
先讨论 WorldWarp 在生成模块中的选型价值，部署细节留到第四章。

### 来源 / 设计依据

- [WorldWarp 本地记录 / 源码](../../archive/WORLDWARP_SETUP.md)
- [本地几何链路验收](../../archive/GEOMETRY_SETUP.md)
- [WorldWarp](https://github.com/HyoKong/WorldWarp)
- [MapAnything](https://github.com/facebookresearch/map-anything)
- [GaME](https://github.com/VladimirYugay/GaME)

## 14｜WorldWarp：可用于几何引导的新视角生成

开场句
在选型层面，WorldWarp 是几何条件和生成结合较紧的一个候选。

展开讲
1. 内部先从已有帧恢复几何并构造局部 GS，按目标相机渲染条件，再由生成模块补全新内容。它不是纯视频模型，也不是纯建图后端。
2. 它可以功能性承担原组装图的生成器位置，与 Spatia 是候选替换关系；没有做质量排名，也不认为二者接口相同。
3. 若使用外部持久 GS，需要替换或协调其内部局部建图及条件构造。具体本地安装、权重、运行与视频效果全部留到第四章。

过渡句
另一个职责是从图片恢复几何，这部分对应 MapAnything。

### 来源 / 设计依据

- [WorldWarp](https://github.com/HyoKong/WorldWarp)
- [WorldWarp 官方方法页](https://hyokong.github.io/worldwarp-page/)

## 15｜MapAnything：统一的图像到几何前端

开场句
MapAnything 解决的是像素在哪里、相机在哪里，而不是画面该如何生成。

展开讲
1. 原模型可接单图、多图和可选的几何信息，本地包装器目前使用 RGB 推理路径。
2. 可导出 posed RGB-D、pointmap 与同批次聚合点云；pointmap 或 PLY 点云不是已经优化完成的 GS。
3. 它可在职责上替代 TTT3R/CUT3R 的几何预测支路，但多帧组织、尺度、相机约定和状态不能直接照搬。长期地图还需要旧坐标锚和历史重叠。

过渡句
几何前端之后，需要一个能更新场景表达的地图后端。

### 来源 / 设计依据

- [MapAnything](https://github.com/facebookresearch/map-anything)
- [MapAnything 官方方法页](https://map-anything.github.io/)
- [本地几何链路验收](../../archive/GEOMETRY_SETUP.md)

## 16｜GaME：增量高斯地图后端

开场句
GaME 承担地图侧的工作，前提是输入已有匹配的 RGB-D、K 和位姿。

展开讲
1. 原生方法利用关键帧与掩码处理新增和过时几何，并优化高斯场景。
2. 本地适配已验证静态多帧建图、保存与重载，但采用全图静态 mask，每次 CLI 调用创建新场景。原生 checkpoint 恢复能力不等于当前 CLI 已接好跨轮续图。
3. 因此它是持久地图开发的后端基础，不是完整 SLAM、未来视频生成器或连续 4DGS 系统。

过渡句
三个职责分清后，才能明确组合替换的是哪一段。

### 来源 / 设计依据

- [GaME](https://github.com/VladimirYugay/GaME)
- [GaME 官方方法页](https://vladimiryugay.github.io/game/)
- [本地几何链路验收](../../archive/GEOMETRY_SETUP.md)

## 17｜组合后怎么替换？保留生成器，重接几何侧

开场句
我们不是用 MapAnything 加 GaME 替换整个 WorldWarp，而是替换和扩展它的几何与局部地图部分。

展开讲
1. 原 WorldWarp 内部是：已有帧经 TTT3R/CUT3R 预测几何，建立局部 GS，再渲染条件交给 WorldWarp 生成模块。生成视频进入下一段历史。
2. 目标组合是：MapAnything 为新帧与历史提供几何，GaME 在统一世界坐标中维护同一场景，渲染得到的条件仍交给 WorldWarp 的生成模块。生成后再选择新帧送回重建。
3. 红线是不能重复运行两套互不一致的几何坐标：外部地图提供条件后，WorldWarp 内部局部几何逻辑需要被明确旁路、替换或协调。渲染可以用 GaME 自带 rasterizer 或统一 gsplat 适配器，不能把二者视作已换好的同一个接口。
4. 这个组合可覆盖单图 / 多图到已观测区域初始 GS 的功能，但纯文本到世界、未见区域生成、多模态统一建模、全局 SLAM 和 4D 动态仍不是三个项目自然相加就具备。

过渡句
在明确替换范围后，按依赖关系安排复现阶段。

### 来源 / 设计依据

- [WorldWarp 本地记录 / 源码](../../archive/WORLDWARP_SETUP.md)
- [本地几何链路验收](../../archive/GEOMETRY_SETUP.md)
- [系统综合图 v2](../../../../diagrams/atlas_architecture_synthesis_v2.svg)

## 18｜复现路线：从几何子链路到持久世界，再到 4D

开场句
复现路线分为三段：先验证几何与资产接口，再闭合静态世界，最后扩展长程和动态。

展开讲
1. 第一阶段先跑通图片到几何、到 GS、到重渲染，固定输入数据格式；这一部分已有本地小样本验证。
2. 第二阶段固定时间，把持久地图接给生成器，新图重建后写回同一地图，并在下一轮复用。首先只做两轮、小幅相机移动。
3. 第三阶段才增加历史长度、返回原处、不同路径及动态表示。初始化与空间上下文始终保留在系统架构内，阶段划分是实施次序。

过渡句
先用已经完成的几何子链路说明第一阶段的实际基础。

### 来源 / 设计依据

- [系统综合图 v2](../../../../diagrams/atlas_architecture_synthesis_v2.svg)
- [原开源组装架构图](../../../../diagrams/atlas_open_source_assembly.svg)
- [本地几何链路验收](../../archive/GEOMETRY_SETUP.md)

## 19｜已有基础：几何写回子链路已完成小样本验证

开场句
这一页展示的是已经发生的事情，而不是下一步计划。

展开讲
1. 两套 Conda 环境、MapAnything 两种权重以及 GaME 的 CUDA 依赖已经安装校验。用三帧既有 WorldWarp 输出跑出 84,245 个合并点，再生成 18,991 个 Gaussians。
2. 三张图依次是第 0 帧的模型输入 RGB、对应预测深度和同相机的 GS 重渲染；尺寸经过统一展示，并不构成画质基准。GaME 测试 max-width 224，迭代数刻意较小。
3. 还做过 GS 渲染图再次输入 MapAnything 和 GaME 的验证，但那次新建了独立场景，不是往同一长期地图继续融合。

过渡句
有了几何子链路，下一步就是接通跨模块、跨轮的三个接口。

### 来源 / 设计依据

- [本地几何链路验收](../../archive/GEOMETRY_SETUP.md)

## 20｜实现接口：条件输入、全局坐标、地图状态

开场句
完整闭环还缺的不是更多项目，而是这三处具体的状态交接。

展开讲
1. 接口 A：从持久 GaME 场景按目标相机渲染，将 RGB、几何有效性和必要深度转成 WorldWarp 接受的条件；需要匹配尺度、分辨率、mask 语义和生成器输入分布。
2. 接口 B：把新帧和有重叠的历史帧联合送给 MapAnything，并让已知相机、坐标锚参与几何推断或后对齐，避免每批次自由漂移。
3. 接口 C：在同一地图上继续 GaME 更新，管理 checkpoint、优化器、关键帧与新增区域检查；失败可以拒绝写入或回退。当前 CLI 没有这个跨调用 resume 路径。

过渡句
接口接上后，用两轮共享地图与回看检查来验收。

### 来源 / 设计依据

- [本地几何链路验收](../../archive/GEOMETRY_SETUP.md)
- [WorldWarp 本地记录 / 源码](../../archive/WORLDWARP_SETUP.md)
- [系统综合图 v2](../../../../diagrams/atlas_architecture_synthesis_v2.svg)

## 21｜验收标准：两轮共享地图，再验证回看一致性

开场句
验收顺序从已有证据出发，不直接跳到长时间 4D 演示。

展开讲
1. 第一关已经完成安装级验证：从图像得到几何、GS，保存后能重载再渲染；这不代表质量达到产品水平。
2. 第二关做两轮小范围相机移动，先固定 t，证明第二轮从第一轮世界状态继续，新增区域被复用、旧区不明显退化。保留地图版本、关键帧和接受 / 拒绝记录。
3. 第三关走 A 到 B 再回 A，加入保留验证视角与不同探索路径，观察回看一致、几何冲突与漂移；有 GT 才报告有意义的重建精度。

过渡句
前三章的系统与复现路线到这里讲完；最后集中看本地 WorldWarp 的部署与实际结果。

### 来源 / 设计依据

- [本地几何链路验收](../../archive/GEOMETRY_SETUP.md)
- [系统综合图 v2](../../../../diagrams/atlas_architecture_synthesis_v2.svg)

## 22｜本地 WorldWarp：部署了什么，已经验证到哪里

开场句
最后用本地 WorldWarp 说明：我们已经具备了怎样一个可运行的生成起点。

展开讲
1. 项目位于 WorldWarp，独立 Conda 环境为 conda_envs/worldwarp，一键 GUI 入口为 run_worldwarp.sh。部署记录包括模型、CUDA 扩展、GUI 和端到端验收。
2. 主要模型包括 WorldWarp 微调权重、Wan2.1 基础生成栈、TTT3R/CUT3R 几何权重以及 Qwen-VL 提示处理。具体版本和检查记录在部署文档中。
3. 9 月 5 日曾有单段 81 帧验证；随后 9 月 7 日增加了教室四段、321 帧测试。本次 PPT 重排只读取已有产物，没有再次生成视频或启动 GPU 服务。
4. 四段完成仍不等于无限长度视频、跨轮永久 GS 地图或完整 Atlas 闭环。

过渡句
先看怎么启动、怎么给输入，再展示实际结果。

### 来源 / 设计依据

- [WorldWarp 本地记录 / 源码](../../archive/WORLDWARP_SETUP.md)
- [WorldWarp 教室四段实测](../../../../WorldWarp_outputs/classroom_pan_left_2026-09-07/README.md)

## 23｜怎么运行：给起始图、相机轨迹和生成设置

开场句
使用时只需把起始图、相机运动和生成设置说明白，部署脚本负责选择本地环境。

展开讲
1. 交互方式是从项目根目录运行 run_worldwarp.sh，浏览器打开对应端口，然后上传图像、选择预设 / 自定义相机运动并生成。运行前选择空闲 GPU 与端口。
2. 若要精确复现本次教室匀速旋转，使用 scripts/generate_worldwarp_rotation.py。该脚本构造同一条连续相机轨迹并正确切分到各 chunk，避免分段重置。
3. 本次已有测试采用 4 段、480×608、固定相机位置、总计左转 20 度，strength 0.6、每段 500 次 GS 优化。输入角度是控制条件，不是由输出画面测得的相机真值。
4. 命令与完整参数放在本目录 WorldWarp 说明文件；这一页只介绍入口，未在本轮启动服务或执行生成。

过渡句
下面直接看这一张教室图产生的四段结果。

### 来源 / 设计依据

- [WorldWarp 本地记录 / 源码](../../archive/WORLDWARP_SETUP.md)
- [本地匀速旋转脚本](../../../../scripts/generate_worldwarp_rotation.py)
- [WorldWarp 教室四段实测](../../../../WorldWarp_outputs/classroom_pan_left_2026-09-07/README.md)

## 24｜真实案例：一张教室图 → 4 段、10.7 秒视频

开场句
左边是预处理后的输入教室图，中间是四段拼接后的真实视频。可以在支持的视频播放器中点击播放。

展开讲
1. 最终文件为 480×608、30 fps、321 帧、10.7 秒。四段各 81 帧，后三段各去掉 1 帧重叠：81 + 3×80 = 321。
2. 输入控制是相机位置保持不动、绕 Y 轴匀速左转，总角度为负 20 度。角度和匀速是轨迹条件，不能等价宣称输出已经达到标定级相机精度。
3. 已有生成报告记录总耗时约 20.1 分钟、PyTorch 峰值分配显存约 33.2 GiB；这是该配置的实际运行记录，不是标准化速度基准或进程总显存。
4. 已有检查完整解码 321 帧，检查了轨迹与三处接缝。新暴露区域、局部纹理和墙面文字仍由模型生成，没有几何精度保证。
5. 视频原文件以相同字节嵌入 PPT，并复制到 assets。当前环境未进行 Office 播放测试；若客户端不支持，可单独打开视频。

过渡句
看完视频，再区分它留下的视频、高斯缓存和复现记录分别有什么用途。

### 来源 / 设计依据

- [WorldWarp 教室四段实测](../../../../WorldWarp_outputs/classroom_pan_left_2026-09-07/README.md)
- [教室生成报告](../../../../WorldWarp_outputs/classroom_pan_left_2026-09-07/report.json)

## 25｜本地留下什么：视频、局部 GS 与复现记录

开场句
一个可复现实验不仅留下视频，还应该留下几何缓存、轨迹和实际运行记录。

展开讲
1. 视频用于直接观察生成效果；每段 GS 主要服务于几何条件构造，不能把它理解为四段所有新内容已经融合后的全局场景。
2. 相机轨迹、report、源代码快照、关键帧和解码检查用于追溯。教室案例的 sources 与 source_manifest 已保留运行时源文件。
3. 下一步接回系统需要：把局部条件来源接到持久 GS；让新帧与历史联合重建；将可信结果写回同一 GaME 场景，再供下一轮使用。
4. 这些是需要继续实现的接口，不是因为 4 段视频生成完成就自动具备的能力。

过渡句
最后把这四个层次收束为同一条实施主线。

### 来源 / 设计依据

- [WorldWarp 教室四段实测](../../../../WorldWarp_outputs/classroom_pan_left_2026-09-07/README.md)
- [WorldWarp 本地记录 / 源码](../../archive/WORLDWARP_SETUP.md)
- [教室生成报告](../../../../WorldWarp_outputs/classroom_pan_left_2026-09-07/report.json)

## 26｜结论：架构确定方向，WorldWarp 提供本地起点

开场句
完整逻辑只有四层：先定义系统，再定义功能，再选择开源实现，最后用本地案例验证起点。

展开讲
1. 总体架构保留多模态初始化、统一世界模型、双路记忆与 4D；功能层说明模块之间的数据和状态。
2. 开源层把生成、几何、地图与渲染分工，并按阶段接线，而不是把三个项目简单串起来就声称完整复现。
3. 本地 WorldWarp 已有四段教室视频验证；结合已有 MapAnything 与 GaME 子链路，下一步应聚焦两轮共享同一地图。

过渡句
主讲结束。技术问答按需查看附录；本地视频也可单独打开播放。

### 来源 / 设计依据

- [系统综合图 v2](../../../../diagrams/atlas_architecture_synthesis_v2.svg)
- [本地几何链路验收](../../archive/GEOMETRY_SETUP.md)
- [WorldWarp 教室四段实测](../../../../WorldWarp_outputs/classroom_pan_left_2026-09-07/README.md)

## 27｜A1｜AR、扩散与场景时间，分别是什么？

开场句
这里澄清三种不同的轴，避免把 AR 和扩散误认为互斥。

展开讲
1. AR 指序列组织：在前面上下文条件下预测下一个元素。官网的元素可以包含不同模态，不能简单限制为按物理时间输出下一视频帧。
2. Rectified flow / 扩散指当前输出元素内部的生成过程，在潜空间中由噪声逐步恢复样本。
3. 物理时间 t 是场景状态查询条件，与序列索引 k、内部去噪步 s 是不同概念。固定 t 换相机仍可逐元素 AR 生成。

过渡句
落到开源组装时，接下来需要统一的不是网络形式，而是中间数据约定。

### 来源 / 设计依据

- [World Labs · Atlas](https://www.worldlabs.ai/blog/atlas)
- [系统综合图 v2](../../../../diagrams/atlas_architecture_synthesis_v2.svg)

## 28｜A2｜统一中间格式：图像、相机、几何

开场句
这页适合交给具体写适配器的人，主讲时无需逐字段朗读。

展开讲
1. 当前 posed_rgbd.npz 保存 rgb、depth_z、valid、intrinsics、c2w、confidence；点图另存 pointmaps.npz。
2. RGB 使用 uint8 NHWC；深度是相机 z-depth，不是射线距离。批次内图像尺寸要统一，改变分辨率时要相应缩放完整 K。
3. 统一相机约定为 OpenCV c2w；进入 GaME 实际执行接口前必须求逆得到 w2c。本地另有完整 K 投影修正，不能删除这个适配器。
4. 独立 MapAnything 调用的世界原点和尺度不能默认一致；长期图需要坐标锚与重叠约束。pointmap 体素合并也不是 SLAM 的回环优化。

过渡句
有了这个格式，两个独立环境可以通过文件接口直接运行。

### 来源 / 设计依据

- [本地几何链路验收](../../archive/GEOMETRY_SETUP.md)
- [MapAnything](https://github.com/facebookresearch/map-anything)
- [GaME](https://github.com/VladimirYugay/GaME)

## 29｜A3｜本地如何运行？两步，两个环境

开场句
两个项目通过 NPZ 文件交换，不需要把依赖硬塞进同一个 Conda 环境。

展开讲
1. 演示命令中的输入和输出目录需替换，GPU 需在执行时自行选择空闲卡；不要直接复用已有结果目录。
2. 两步都写 --max-views 3 是为了让实际使用帧数相同。默认 MapAnything 是 8，GaME 是 3；不改参数可能只让 GaME 使用前 3 帧。
3. 三帧是当前小测试设置，不是模型只能接三帧。两步至少一帧可走，但重叠多视角更适合验证几何一致性。
4. 当前包装器不是无限流式输入：更长序列受模型上下文、显存、地图大小约束。长期运行需窗口、关键帧筛选、持久状态和资源预算。GaME 原生 additions 变化分支还需更多已接受关键帧触发，三帧测试不覆盖所有分支。

过渡句
核心链路明确后，才有必要按瓶颈比较其他可选组件。

### 来源 / 设计依据

- [本地几何链路验收](../../archive/GEOMETRY_SETUP.md)

## 30｜A4｜主链路之外，按瓶颈选备件

开场句
这一页是替换目录，不建议在闭环未接通时把所有项目一起安装。

展开讲
1. 初始世界可继续评估 HY-World 2.0；生成器保留 Spatia 原研究主候选。它们都不在本次已完成的几何安装范围内。
2. DA3 可作几何对照，SAM 3 可辅助实例掩码；分割实例不自动等于识别运动。NoPo4D 是时空重建候选，不替代未来运动生成。Spark 负责场景浏览展示，不是地图融合器。
3. 正式选型应按锁定版本核对接口、许可证和权重授权。MapAnything 有非商业默认权重与 Apache 替代权重；GaME 依赖中的许可也要单独检查。此页不新增“近一年发布”的未经复核断言。

过渡句
如果讨论 4D，最先需要明确的其实是输入覆盖哪些相机和时刻。

### 来源 / 设计依据

- [既有开源组装研究](../../archive/ATLAS_OPEN_SOURCE_ASSEMBLY_2026-09-06.md)
- [本地几何链路验收](../../archive/GEOMETRY_SETUP.md)

## 31｜A5｜4D 的关键，是覆盖“相机 × 时间”

开场句
这个网格表示观测组织方式：一列是同一时刻的多相机，一行是同一相机的时间序列。

展开讲
1. 不同视角、同一时刻用于约束该时刻的空间几何；同一视角、多个时刻用于约束运动和变化。每一个格子可以来自真实采集，也可能来自生成候选，但两类证据的可靠性不能混为一谈。
2. 单相机的一行视频缺少其他视角，并不能唯一确定遮挡后或背面的动态几何。要生成可自由换视角的 4D 资产，就要补多视角证据或引入额外深度、运动与结构先验。
3. NoPo4D 这类候选负责从时空观测重建动态表达；它不是独立决定未来物体怎么运动的生成器。

过渡句
接着用能力矩阵与输入限制，把三个项目的职责边界收束清楚。

### 来源 / 设计依据

- [NoPo4D](https://github.com/bralani/NoPo4D)
- [系统综合图 v2](../../../../diagrams/atlas_architecture_synthesis_v2.svg)
- [World Labs · Atlas](https://www.worldlabs.ai/blog/atlas)

## 32｜A6｜能力边界：能覆盖、要适配、不能替代

开场句
这张矩阵按任务而不是按项目宣传口号来读。

展开讲
1. 生成新视角与补全画面主要由 WorldWarp 承担；MapAnything 与 GaME 处理已有图像或深度，无法替代生成新内容的职责。
2. MapAnything 对应几何前端，GaME 对应地图后端。两者组合可以做局部场景初始化，也能为后续增量地图开发提供基础，但当前程序跨调用还没有保持同一世界。
3. 相机跟踪、全局回环、候选真实性审核和连续 4D 都没有因三者串联而自动完成。GaME 的变化适应也不能当作未来预测器。

过渡句
最后再把实际输入帧数、掩码与跨轮状态限制列清楚。

### 来源 / 设计依据

- [WorldWarp 本地记录 / 源码](../../archive/WORLDWARP_SETUP.md)
- [本地几何链路验收](../../archive/GEOMETRY_SETUP.md)
- [既有开源组装研究](../../archive/ATLAS_OPEN_SOURCE_ASSEMBLY_2026-09-06.md)

## 33｜A7｜输入与状态：三帧只是测试，不是无限流

开场句
帧数、输入类型和状态恢复分别是三个问题，不能只用一个“支持多少帧”概括。

展开讲
1. WorldWarp 用户可只提供一张起始图；内部 chunk 有帧数和 VAE 时间压缩约束，具体配置需一起校准。本地已有单段 81 帧和教室四段 321 帧验证；这仍不是无限长视频、所有轨迹或持久全局地图的验证。
2. MapAnything 当前输入至少一张，默认最多取 8 张；GaME 包装器默认只取前 3 张。两个 max-views 都要改成相同目标帧数，并注意 GaME 还会筛关键帧。多帧并不要求按固定 3 帧或 8 帧分组。
3. MapAnything memory-efficient 选项降低开销，不等于无上限在线流。静态重建优先同一时刻、多视角有重叠；动态帧盲目当静态多视图会出现冲突。
4. 原生 GaME 在已有超过 2 个关键帧后才进入 detect_additions 分支，因此前三个关键帧测试没有覆盖完整变化检测；但前三帧仍会创建 / 增补 GS 和优化，不是说前 3 帧完全不融合。
5. 跨轮运行要恢复优化器和关键帧相关状态、绑定世界坐标，并筛选可信生成区域。本地 wrapper 现阶段每次新建图，没有无限续图 CLI。

过渡句
资料页给出本地代码与记录入口，需要进一步落地时从这些证据开始。

### 来源 / 设计依据

- [本地几何链路验收](../../archive/GEOMETRY_SETUP.md)
- [WorldWarp 本地记录 / 源码](../../archive/WORLDWARP_SETUP.md)
- [MapAnything](https://github.com/facebookresearch/map-anything)
- [GaME](https://github.com/VladimirYugay/GaME)
- [WorldWarp 教室四段实测](../../../../WorldWarp_outputs/classroom_pan_left_2026-09-07/README.md)

## 34｜A8｜资料与证据：按这个顺序继续读

开场句
阅读顺序同样从整体到细节：先总图，再实现，再源码。

展开讲
1. 官网用于确认模型形式与公开能力；综合图 v2 用于保留既有世界闭环假设；开源调研用于了解项目角色；本地安装记录用于确认哪些东西实际运行过。
2. PPT 每页有相应来源或设计依据，详细讲稿也附了链接。源代码级定位见 README 的证据索引。
3. 前两版 PPTX、PDF 和 diagrams 中已有 SVG 均保留；新版单独放在新的目录中。这里的 PDF 与 PPTX 共用布局生成，但不是用 PowerPoint 或 LibreOffice 渲染导出的 PDF。

过渡句
后续讨论直接回到某一功能或接口，不必再从完整项目清单开始。

### 来源 / 设计依据

- [World Labs · Atlas](https://www.worldlabs.ai/blog/atlas)
- [系统综合图 v2](../../../../diagrams/atlas_architecture_synthesis_v2.svg)
- [本地几何链路验收](../../archive/GEOMETRY_SETUP.md)
