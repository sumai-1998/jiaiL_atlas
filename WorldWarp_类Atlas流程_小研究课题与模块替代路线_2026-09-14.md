# WorldWarp 类 Atlas 流程：小研究课题与模块替代路线

初稿日期：2026-09-14。最新研究优先级复审：**2026-09-19**（依据 2026-09-18 的流程核查与实验）。

> 本次更新依据 2026-09-18 的源码复核、五场景组合实验和单场景基线重验。**以下“2026-09-19 复审”是当前建议；后面的 12 项为保留的历史候选库，不再代表当前优先顺序。** 新增内容是研究判断与待验证设计，没有新增 GPU 实验，也不声称提出了尚无人研究过的方法。

## 2026-09-19 复审：真正值得研究的问题

### A. 当前判断：从“替换模块”收敛到“闭环中的错误如何产生和传播”

最值得凝练的总问题是：

> **在仅有首张真实图像、后续观测由视频模型自己生成的条件下，怎样让几何重建、目标相机控制与视频生成保持一致，并避免生成错误被后续重建固化？**

这比“把 MapAnything、GaME、WorldWarp 串起来”更能形成可检验的研究目标。模块串联是实验平台；研究贡献需要说明具体失效机制、针对性方法和公平验证。

当前建议集中为 **三个主方向，加一个有条件开展的效率方向**：

| 优先级 | 研究问题 | 对应旧课题 | 当前判断 |
|---|---|---|---|
| P0：实验前提 | 把相机、尺度、输入历史和评价接口核对清楚，定位错误在哪一阶段出现 | 8 的实现核查、10 的无损历史、分阶段诊断 | 必须先做；常规修错不作为算法贡献 |
| P1：首先开展 | 生成画面、重建相机和目标相机之间的偏差，如何被检测并限制其跨段传播？ | 8，部分 7 | 最贴合当前“视角不对、后期结构崩坏”的现象 |
| P1：与上项分开验证 | 已有粗渲染应保留多少、允许修正多少，如何由可靠性决定？ | 9，部分 2、3 | 比单纯降低噪声更明确，适合短段受控实验 |
| P2：形成长期主线 | 自生成历史如何作为可撤销的几何假设写入持久记忆，避免错误反复强化？ | 4＋5，配套 3、6、7 | 最接近类 Atlas 的目标，但先证明写入策略，再扩展地图 |
| 条件开展 | 怎样在固定端到端预算下减少几何更新成本，同时保持上述质量？ | 1＋11，部分 2 | 先做本地分阶段计时；前馈替换本身不构成充分创新 |

### B. 最近复核确认了什么，尚未确认什么

| 证据 | 能支持的判断 | 不能据此下的结论 |
|---|---|---|
| 原版每段新建 GS；首段源图 1 张，后续 GS 用上一段末尾连续 5 帧；TTT3R 处理上一段完整 49 帧 | 几何主要通过生成 RGB 跨段传递，存在重新估计和信息遗忘的入口 | 不能断言“只要继承旧 GS 就会改善” |
| 原版已有 alpha 与源视图深度一致性检查，扩散已有有效/无效区域不同噪声 | 已有几何支持与噪声调度是强基线 | 不能把“增加一个 mask”或“空间不同噪声”作为新贡献 |
| GS 在当前段扩散之前拟合并保存 | 当前 PLY 没有吸收本段随后生成的新帧；最后一段没有额外全局重建 | 不能将五个 PLY 当成同一全局地图的五次优化 |
| 五场景 F/G/H 和三项目没有全面优于本地原版；部分画面更清楚但视角不符 | 外观清晰、相机控制与几何正确性必须分开评价 | 不能据此证明 MapAnything 或 GS 本身较差；多因素同时变化 |
| 补回参考内参平均后，单场景重验仍失败；标定相机诊断也仍失败 | 仅修这一处接口或替换目标相机不足以解决当前问题 | 不能认为相机与尺度链条已全部正确，更不能反推论文方法无效 |
| 微调权重文件和 825 个加载张量核验通过 | 已排查这两次运行的权重错文件/未加载问题 | 不代表预处理、相机接口、采样协议和质量复现都正确 |
| 粗渲染已出现失真，扩散后又有主体/结构变化 | 应同时检查几何条件质量和生成阶段的条件遵循 | 未隔离各因素，不能宣布某阶段是唯一根因 |

详细依据：[完整实际流程](docs/WORLDWARP_DETAILED_WORKFLOW.md)、[五场景评测总结](docs/WORLDWARP_PIPELINE_EVALUATION_SUMMARY.md)、[单场景重验报告](Reports/dl3dv_worldwarp_recheck_20260918/README.md)。

尤其要更正旧文中容易混用的“当前流程”：旧文主要讨论 **G / anchor_gs**，不是原版。G 的几何来源是原图锚点加均匀历史关键帧；原版后续使用末尾连续 5 帧。旧文“固定请求位姿拟合”主要描述 MapAnything 桥接分支；原版在后续重建中使用 TTT3R 估计的历史相对位姿，不能统称所有分支都用请求位姿建模。

### C. 第一主方向：相机控制与生成几何的一致性

**建议问题表述：生成视频没有完全服从目标轨迹时，如何避免重建把运动偏差错误解释成场景形变，并在下一段继续放大？**

需要显式区分：

- `C_target`：不可为追求更好分数而事后改动的目标相机轨迹。
- `C_recon`：用于解释源图并拟合几何的相机估计。
- `C_eval`：由独立评价方法从输出估计的相机；存在估计误差和失败情况。
- 数据集标定相机：有配对数据时的参考真值。它与原版 TTT3R 估计的目标轨迹不一定完全一致，应同时报告二者偏差。

**待验证假设：** 生成帧的运动偏差与窗口间几何/尺度变化，会使下一段条件偏离请求轨迹；对可靠共视区域做受限对齐、对不可解释的源区域降权，可以减小这种传播。目前只观察到了相关现象，尚未证明因果贡献大小。

最小实验分两层：

1. **固定来源回放**：固定首图/历史 RGB、深度、内参和目标视角，只改来源位姿解释或共视区域的尺度对齐；比较粗渲染、重投影残差与拟合形变。原版保留“TTT3R 历史位姿”基线，G 保留“请求来源位姿”基线，分别报告。
2. **两段闭环**：第一段完全相同，只在进入第二段时启用校准/拒绝策略，查看第二段能否更好遵守同一目标。有效后再扩大到五段、多个场景和种子。

至少包含：不改动、仅尺度处理、仅来源位姿处理、二者组合。所有分支使用相同输入和目标轨迹；不要同时更换几何模型、mask、文本与优化步数。

可研究的技术点是**可靠共视约束、受限位姿校准和失败时拒绝更新的联合机制**。不应靠把 `C_target` 换成 `C_recon` 来“消除误差”，那只是改变任务。

验收同时看：目标轨迹偏差、粗渲染结构、生成结构、后续漂移。仅重投影误差降低不够，错误几何也可能通过自由调整相机把图像拟合得很好。纯旋转下不要估计不可辨识的绝对尺度；平移尺度实验需要有足够共视和视差。

**停止或降级条件：** 若固定输入的校准只提高源图拟合，却没有改善新视角或闭环控制，不能将其作为有效生成方法；保留为诊断工具。若收益完全来自纠正 c2w/w2c、内参变换或单位错误，应归类为实现修复。

### D. 第二主方向：可靠性如何控制扩散对几何条件的修改

**建议问题表述：粗渲染既可能含有正确结构，也可能含有几何错误，怎样让扩散保留前者、修正后者，而不是统一地信任或抹掉它？**

WorldWarp 已经提出有效区域部分噪声、空洞区域完整噪声的时空调度，因此“按区域加不同噪声”本身不是新贡献。见 [WorldWarp 原论文](https://arxiv.org/abs/2512.19678)。

可进一步研究的是：**几何支持是否真的可靠，以及该可靠性与生成后结构保真之间的关系。** 原版 mask 已包含深度一致性，不能只与 alpha-only 的弱基线比较。

建议分别记录三个信号，而不是把所有东西直接乘成一个不透明分数：

| 信号 | 例子 | 限制 |
|---|---|---|
| 几何支持 | 深度一致性、投影残差、遮挡边界 | 预测彼此一致仍可能共同出错 |
| 来源可靠性 | 首图观测、生成段龄、共同来源链 | 原图的 RGB 可靠不代表其单目深度是真值 |
| 条件敏感性 | 轻微扰动深度/位姿后渲染是否剧变 | 稳定性是风险信号，不直接等同正确概率 |

最小实验先冻结粗 RGB、文本、上下文、目标相机及种子，比较：

1. 原版几何有效性二值 mask 和原采样配置。
2. 全局 strength 扫描，检查收益是否只是整体少加噪。
3. 仅改进二值 mask，保持调度不变。
4. 在同一可靠性定义下使用三级或连续噪声安排。

按“已知且可靠、已知但冲突、无支持新区域”分别评价。无隐藏区域 GT 时，只报告生成质量和一致性；有配对 GT 时，将新区域匹配作为特定数据集上的结果，不能把单图未观测区域的某一种真实外观视作唯一合理生成。

可以先用人工注入的深度偏差、局部错位和错误纹理做小型可控测试，确认策略能保留正确条件、释放错误条件，再跑真实失败案例。精确几何可用合成场景提供，仅作单独标记的诊断上限，不进入单图方法的正式输入。

还需注意训练分布：当前权重训练时的噪声安排，不自动覆盖任意连续分数。先做推理消融；若要微调，应构造已知扰动与正确对应的训练样本，并划分独立训练/验证场景，不能只拟合五个测试场景。

**停止或降级条件：** 若三级调度与最优全局 strength 等效，或只让图像更清楚而运动更差，不足以支撑新方法。保持、修复、新区域生成三者必须联合报告。

### E. 第三主方向：可撤销、带来源的持久空间记忆

**建议问题表述：没有新增真实观测时，如何记住已经生成的世界，同时不让一次幻觉成为永远无法纠正的地图事实？**

该方向把旧课题 4 与 5 合并为主线：**先决定能不能写，再决定怎样融合和持久保存**。外观双缓存、选帧和旧深度先验成为配套机制。

简单继承旧 GS 可能提高稳定性，也可能永久锁住首轮错误；仅把首图权重设为 2 不能区分各区域的可信程度。高斯数量从 5 万变成 25 万，也不代表信息更真实。

推荐的最小状态：

```text
观测外观：首图 RGB 与可见区域，优先保留
几何假设：深度/高斯及其坐标与不确定性，可被修正
生成候选：新区域的外观和几何，带生成段与祖先来源
更新记录：接受、冲突、拒绝与可回滚的局部修改
```

同一段 5 张相似生成帧不是 5 份独立证据；即使跨段重复，也可能来自同一祖先错误。候选通过回访只能提升**内部一致性**，不能升级为“真实观测”。

最小原型不必先移植完整 GaME：先用关键帧库＋局部 GS 候选＋来源标签，在单房间、静态、探索后返回的轨迹上比较以下 2×2 对照。

| 地图生命周期 | 不做来源门控 | 做来源门控 |
|---|---|---|
| 每段重新拟合 | 当前滚动方案 | 根据来源可靠性选择/加权建模，隔离门控收益 |
| 持久保存并更新 | 直接融合新生成几何，隔离持久性的收益与风险 | 带来源、冲突处理和回滚的持久更新 |

四组保持相同候选历史、活动点数和优化预算，另报告持久库的累计内存/磁盘成本。最终必须在各自生成历史的闭环里再比较，不能只用同一组较好的历史离线回放。

核心测量包括：回到首图/旧视角后的结构与外观误差、注入错误对后续区域的影响范围、可靠旧区域被错误覆盖的比例、新区域的接纳能力、回滚后恢复程度。错误率需要有人工注入标签或独立参考，不能只用方法自己的 confidence 定义正确答案。

“持久 3D 地图指导生成”已有相关研究，例如 [Learning 3D Persistent Embodied World Models](https://arxiv.org/abs/2505.05495)。因此研究增量应落在**相关生成来源的证据管理、错误可撤销、保持与修正的平衡**，不能仅写“首次加入三维记忆”。该文献核查不足以证明这些细化设计全新，正式立项还需逐项比较相关方法。

**停止或降级条件：** 若简单保留首图和几个 RGB-D 关键帧就能取得同等回访效果，而复杂持久 GS 没有额外收益，应采用简单方法，并放弃“必须持久高斯”的前提。

### F. 哪些方向降级，以及为什么

| 原候选 | 调整后定位 | 升级为研究方向的条件 |
|---|---|---|
| 换成 MapAnything / 前馈 GS | 替代基线与效率实验 | 在相同输入、预算和控制协议下，证明特定失效机制被解决 |
| 前馈 GS＋少量优化 | 有条件的效率主线 | 实测几何阶段确有瓶颈，并形成端到端质量—耗时优势 |
| 外观与几何双缓存 | 长期记忆的配套，纯旋转下可先做 | 不只是更锐利，还改善回访与已知区域保真，含平移时遮挡正确 |
| 表面感知初始化、关键帧选择 | 小型消融/基线组件 | 在相同点数、图像数、步数下有可重复收益，能解释机制 |
| 无损 RGB 历史 | 工程卫生与误差诊断 | 单独研究需要证明新的缓存一致性机制，而非仅去掉 MP4 压缩 |
| 潜空间记忆 | 后置且需重新查新 | 已有 [Latent Spatial Memory for Video World Models](https://arxiv.org/abs/2606.09828)；不能仅靠“把 RGB 缓存改 latent”立论 |
| 动态 GaME / 对象分层 | 当前静态任务后置 | 换成确有动态观测的任务，明确区分运动与生成错误 |
| 直接大规模微调视频模型 | 尚无针对性，不先开展 | 确认限制来自条件遵循，并有明确数据、损失与独立验证集 |

GaME 的动态更新不是当前静态场景的必要条件；其融合与地图管理代码可以借鉴。是否采用它，应由接口成本和对照收益决定。

本轮只核对与创新边界直接相关的上述论文；后文历史项目列表的模型规格、许可证和最新可用性没有逐项重新核验，不将它们当作本轮推荐安装清单。

### G. 下一步实验顺序与验收门槛

**阶段 0：先让实验可解释。** 对现有失败场景检查固定 K、c2w/w2c、裁剪变换、平移单位、分段索引、上下文来源和源图质量。内参平均已修正，不重复把它当作未做工作；仍需验证局部重建与目标轨迹的几何兼容。现有权重核验结果可复用。

保存或复用四级输出：`源图/深度/相机 → GS 粗渲染/mask → VAE 往返 → 最终生成`。按首次明显偏差出现的位置定位；不能将不同图像表示之间的任意差值直接叫作真值误差。最初只取首段与第一次跨段，无须立即重跑全部五场景。

**阶段 1：相机与条件遵循的两个独立小实验。** 分别验证 C 和 D，不一开始就把校准、门控、持久地图和新几何模型一起加入。固定文本条件，或至少把自动 caption 的变化作为显式因素；相同 seed 配对，并扩大到多个种子，避免把随机波动当作收益。

**阶段 2：来源门控与持久记忆的 2×2 实验。** 使用纯旋转回访和带平移回访两类轨迹；前者测外观保持，后者检验几何和遮挡。与简单 RGB-D 关键帧记忆比较，判断是否真的需要持久 GS。

**阶段 3：才做效率替换或视频微调。** 先测几何推理、GS 拟合、渲染和扩散占比；如收益集中在扩散条件遵循，则围绕可靠性监督训练；如几何时间确实占主导，再接前馈 GS。不能拿论文硬件的耗时占比代替本地 profile。

评价必须保留现有无损全图 PSNR/SSIM/LPIPS 和相机指标，同时增加模块级、区域级、回访与错误传播诊断。DUSt3R 在退化图像上也可能失败，需报告重建成功率、匹配支持等诊断；不能靠事后配准、修改目标轨迹或删除失败场景来提高正式分数。五场景端点 FID 只作诊断，不能作为主结论。

目前这些场景已多次查看和用于设计方法，应明确作为开发集；若后续提出方法，需要预先留出未用于调参的测试场景。不得在看到新方法结果后反复挑选“有效”的测试轨迹。

**当前基线仍未通过质量验收。** 可以继续做受控机制研究，但以“优于 WorldWarp 论文基线”为目标的正式结论，必须先建立协议清楚、质量经过验收的基线；若仍无法完成，应明确限定为本地适配系统上的结果。

### H. 当前建议凝练的课题名称

若先做一个范围可控的小课题，建议：

> **几何引导视频生成中的可靠性校准与条件保持：面向分段自回归的误差传播抑制。**

先选“来源位姿校准”或“可靠性噪声调度”中的一个作为主要干预，另一个保持原样；通过受控错误、短段回放和完整闭环证明作用。

若目标是较完整的类 Atlas 研究主线，建议：

> **单图生成式空间探索中的可撤销三维记忆：相关生成观测下的可信更新与回访一致性。**

相机一致性和条件可靠性是其基础；持久 GS 是可选表示，不是必须预先决定的答案。“类 Atlas”在本文只表示持久探索与回访目标，不声称还原任何 Atlas 产品内部实现。

---

## 历史候选库：2026-09-14 原始路线（优先级已被上文替代）

依据：[单 GT 首帧到类 Atlas 闭环的完整流程](WorldWarp_单GT首帧到类Atlas闭环_完整流程_2026-09-14.md)、当前本地桥接代码、已完成的教室实验，以及本轮核实的项目官方仓库与论文。

## 先说结论

**前馈高斯非常相关，但它主要解决“怎样更快、更好地建出当前这份 GS”；要让类 Atlas 管线长期不漂、不糊，还需要研究“记住什么、相信什么、何时更新、如何把记忆交给生成器”。**

我建议优先关注五个方向：

1. **外观与几何分开的双缓存**：原图和优质关键帧负责保住纹理，GS 负责空间和遮挡。
2. **可信度控制生成强度**：不要把“有高斯覆盖”直接等同于“应该相信这里”。
3. **生成内容的可信写回**：新生成的东西可以进入地图，但不能轻易覆盖更可靠的旧内容。
4. **前馈 GS＋少量局部优化**：替换每轮从零初始化、优化 500 步的部分。
5. **持久 GS 的局部增量更新**：旧区域沿用，只处理新增或冲突区域。

如果目标是尽快改善当前教室案例的画质，我会先做第 1、2 项；如果目标是形成一个相对独立的几何加速课题，先做第 4 项；如果目标是更接近长期可探索的世界，再推进第 3、5 项。

下文共列 **12 个课题**。每个课题都明确接入位置、最小实验、可用项目和研究边界。它们是研究建议，不是声称本地已经实现，也不等同于 Atlas 官方公布的内部模块。

## 1. 先把可研究的位置标出来

此历史章节讨论的主线是 `anchor_gs`，而不是 WorldWarp 原版或固定 GaME 场景分支：

```text
GT 首图 / 已生成历史
        │
        ▼
A. 选择来源帧                    ← 课题 6：关键帧和回访记忆
        │
        ▼
B. 恢复深度与三维几何            ← 课题 7、8：历史几何先验、相机校准
        │
        ▼
C. 初始化、拟合、保存 GS         ← 课题 1、2：前馈 GS、表面感知初始化
        │                         课题 4、5：可信写回、持久增量缓存
        ▼
D. 渲染粗 RGB 与有效性           ← 课题 3、9：外观双缓存、可靠性与遮挡
        │
        ▼
E. WorldWarp 生成新视频段         ← 课题 9、10：噪声调度、潜空间记忆
        │
        └── 回到 A               ← 课题 11：何时更新缓存

以后加入真实时间变化            ← 课题 12：静态 / 动态分层
```

这里有三处特别好的研究切口：

- 当前 GS 每段重新初始化，旧高斯没有跨轮持续继承；因此增量缓存有明确替换对象。
- MapAnything 的 `confidence` 已保存，`observed` 已区分 GT 与生成图，但还没有形成逐像素、逐高斯的可信度管理；因此不需要从零建立所有信号。
- GS 渲染和 WorldWarp 之间已有 RGB＋覆盖接口；许多改进可以先不重训视频模型，只替换生成条件。

具体依据见 [几何桥接器](scripts/worldwarp_map_geometry.py)、[RGB-D 桥接器](scripts/mapanything_worldwarp_rgbd.py) 和 [WorldWarp 采样入口](WorldWarp/pose_control.py)。

## 2. 12 个课题总览

“小 / 中 / 较大”指相对当前代码的接入与验证工作量，不是工期承诺；“无需训练”不排除当前场景上的 GS 优化。

| 编号 | 小课题 | 主要解决什么 | 接入位置 | 最小版本是否重训网络 | 工作量 |
|---|---|---|---|---|---|
| 1 | 前馈 GS＋少量残差优化 | 每轮建缓存太慢，初始化不佳 | B～C | 不需要 | 中 |
| 2 | 表面感知、边缘保真的 GS 初始化 | 细结构丢失、颜色外溢、浮点 | C | 不需要 | 小～中 |
| 3 | 几何缓存＋高保真外观缓存 | 反复重建后纹理越来越糊 | D | 不需要 | 小～中 |
| 4 | 带来源记录的可信地图写回 | 生成错误被反复强化 | C 的更新策略 | 不需要；可再学小门控 | 中 |
| 5 | 持久高斯的增量融合与预算管理 | 旧地图重建、遗忘、规模增长 | C 的存储与融合 | 不需要 | 中～较大 |
| 6 | 质量与信息增益驱动的关键帧选择 | 均匀选帧重复多、漏掉好帧 | A | 不需要 | 小 |
| 7 | 旧地图约束的新几何预测 | 跨轮深度与尺度反复变化 | B | 不需要起步 | 中 |
| 8 | 请求相机与生成画面的轻量校准 | 图像不完全遵循请求位姿 | B～C | 不需要 | 中 |
| 9 | 遮挡与可靠性感知的噪声调度 | 可靠区域被改写，错误区域被保留 | D～E | 不需要起步 | 小～中 |
| 10 | 低损耗上下文与潜空间记忆 | 编解码、重采样造成累积损失 | E 与历史接口 | 不需要起步 | 小～中 |
| 11 | 事件驱动的缓存更新 | 每段都重建，算力分配不合理 | 闭环调度 | 不需要 | 小～中 |
| 12 | 静态背景与动态对象分层缓存 | 动态被错误烘焙到静态 GS | 几何、地图和渲染 | 分割原型不需要 | 较大，后置 |

## 3. 最直接相关的六个缓存课题

### 课题 1：前馈高斯初始化＋少量残差优化

**核心问题：能不能把“预测深度→点云初始化→500 步 GS 优化”改成“一次前馈→必要时修几十步”？**

可以拆成两条接法：

- **首轮单图初始化**：`I₀ → 单图前馈 GS → 渲染条件 → WorldWarp`。
- **后续多视图重建**：`I₀＋选定的生成历史 → 多视图前馈 GS → 局部修正 → 渲染条件`。

#### 可直接拿来做基线的项目

| 项目 | 官方已提供的相关能力 | 在这里怎么用 | 需要注意 |
|---|---|---|---|
| [SHARP](https://github.com/apple-aiml-research/ml-sharp) | 单张照片直接预测可渲染 3DGS，提供推理代码与权重 | 首轮单图 GS；也可测试单个新关键帧的局部 GS 提案 | 面向附近视角，不自动完成多轮全局融合；逐图批处理不等于多图联合建图 |
| [Depth Anything 3](https://github.com/ByteDance-Seed/Depth-Anything-3) | 图像几何预测、相机条件，以及特定模型的 GS 分支 | 同一前端比较“深度→原生 GS”和“直接 GS” | GS 不是每个型号都有；要核对模型卡与 `infer_gs` |
| [AnySplat](https://github.com/InternRobotics/AnySplat) | 未标定多图输入，预测 GS 和相机，提供权重及后优化入口 | 后续历史窗口的前馈缓存 | 预测相机坐标系需要对齐请求轨迹；多图能力不代表无界流式地图 |
| [FreeSplatter](https://github.com/TencentARC/FreeSplatter) | 稀疏未标定视图重建 GS，并恢复相机 | 多视图替代基线 | 当前是房间场景，应选场景版 `FreeSplatter-S`，不要误用物体版 |

时间上，SHARP 为 2025 年 12 月工作，DA3 为 2025 年 11 月发布；AnySplat 与 FreeSplatter 的首次公开更早。这里按模块适配性选项目，不把“2025 年会议”一律算成最近一年发布。

DA3 的具体接入要点：当前官方模型卡中 Giant / Nested Giant 系列有 GS 能力，Large / Base / Small 不能因为名字同属 DA3 就当作带 GS head；官方也已标出部分 `-1.1` 更新权重。API 支持提供相机、开启 GS 和指定渲染视角，外参采用 w2c。见 [模型卡](https://github.com/ByteDance-Seed/Depth-Anything-3#-model-cards) 和 [GS API](https://github.com/ByteDance-Seed/Depth-Anything-3/blob/main/docs/API.md)。

#### 最小实验

先固定一批已经保存的来源图和目标相机，不重跑整个闭环：

1. 原始 RGB-D 初始化＋500 步 GS，作为现有基线。
2. 原始 RGB-D 初始化＋较少步数，作为“只是少优化”的对照。
3. 前馈 GS，不做后优化。
4. 前馈 GS＋20 / 50 / 100 步后优化，比较质量—耗时曲线。

通过静态回放筛选后，再运行真正的闭环，确认优势不会在生成历史分布上消失。

**值得研究的点**不是“换成 SHARP”本身，而是：面对 GT 与生成历史混合的输入，如何用可信度只修正必要的高斯，并在给定总耗时下维持多轮稳定性。

**边界：** 前馈几何模型不是新区域的视频生成器，也不是持久融合器。单图先验预测出的隐藏表面仍是假设，不能直接标成真实观测。整体加速必须测 MapAnything、GS、渲染、视频去噪的实际占比，不能把 GS 加速倍数写成整条管线加速倍数。

### 课题 2：表面感知、边缘保真的 GS 初始化

**核心问题：同样的点数和优化步数，能不能少一些糊边、大高斯和细结构缺失？**

当前初始化每个来源最多随机取 50,000 个点，再根据邻域距离设置初始尺度。可以保留 MapAnything 和整个 WorldWarp，只改点的选择与高斯形状。

最小改法：

1. 对深度连续区域估计局部表面法线；跨深度断层的邻域不参与法线拟合。
2. 沿表面切向设置较大尺度，沿法向设置较小尺度，替代各向同性起点。
3. 限制深度边界附近高斯的屏幕投影尺寸，减少前景颜色向背景漏出。
4. 在可信细结构、纹理边缘增加采样，平坦重复区域减少采样；总点数保持一致。

可复用当前 `GS3DWarper` 与 [gsplat](https://github.com/nerfstudio-project/gsplat)。表面表示和正则化可参考 [2D Gaussian Splatting](https://github.com/hbb1/2d-gaussian-splatting)，但“受法线约束的 3DGS”与真正的 2DGS 不是同一种参数化，后者需要对应渲染器。

**最小实验：** 固定来源、深度、点数、500 步预算，只替换初始化；比较桌椅腿、黑板字边缘等局部渲染，以及高 Alpha 却无真实来源支持的外溢区域。

**研究增量：** 为生成闭环设计“表面可靠性＋屏幕边缘风险”联合点预算，而不是仅增加高斯数。高梯度也可能来自噪声和错误深度，不能看到边缘就无条件加点。

接入：[原生 GS 初始化与训练](WorldWarp/src/ttt3r/ttt3r.py) 中 `_initialize_splats_from_depth`、`_train_splats`。

### 课题 3：几何缓存与高保真外观缓存分开

**核心问题：为什么首图里本来清楚的纹理，要随着每轮 GS 拟合和视频生成被重新压缩一遍？**

我认为这是最贴近当前“越生成越软”的课题之一。建议让两种记忆分工：

- **几何缓存**：GS 保存表面位置、可见性、遮挡和粗颜色。
- **外观缓存**：保留 GT 与优质关键帧的原始 RGB、相机、深度和来源记录。

渲染目标条件时，先由 GS 判断哪里可见，再从合适的关键帧取高质量颜色。可信且视角相近的来源优先；没有可靠纹理的地方才退回 GS 粗颜色。

#### 从当前纯旋转案例起步

可以先做一个非常小的版本：用已有旋转单应关系直接投影 GT / 历史 RGB，构造外观条件；GS 保持不变，比较以下三组：

1. 只有 GS 颜色。
2. 只有旋转图像重投影，作为当前轨迹下的强外观基线。
3. GT / 关键帧重投影＋GS 补缺，并处理边界与来源切换。

当前 `rotation_source_support` 只投影掩码，**并没有做这里建议的 RGB 外观缓存**。纯旋转下这个实验不依赖深度精度，可以较干净地分离“纹理保存”和“几何预测”两个问题。

进一步引入平移时，必须改用深度 / 表面投影与遮挡验证；旋转单应版本不能直接推广。

较新的参考是 [LGTM：Less Gaussians, Texture More](https://github.com/apple-aiml-research/ml-lgtm)（ICLR 2026）：用带纹理的紧凑高斯表示高频外观，官方有权重与推理例子，但采用定制 textured 2DGS 渲染，需要改渲染后端，不能直接当作当前 SH 高斯参数读入。它是表示层参考，不是已经替你实现 GT 优先缓存。

**验证：** 在 GT 能投影到的区域评估纹理保持、感知误差和接缝；在回访视角检查桌椅、文字是否保持。4K 纹理表示也不意味着当前 480×608 视频链会输出 4K，仍受处理分辨率与 VAE 限制。

**研究增量：** 在生成闭环里把几何可修正与外观来源保真分开，并学习或设计稳定的来源选择。要保护的是 GT 的观测外观，不是把首轮单目预测深度永久冻结。

### 课题 4：带来源记录的可信地图写回

**核心问题：WorldWarp 新生成一张图后，哪些内容可以被记住，哪些内容不能覆盖旧地图？**

当前 GT 在 GS 训练里采样权重为 2、生成来源为 1，但这不是逐区域的写入权限控制。可以从现有 `observed` 字段扩展出：

```text
每个图像区域 / 高斯记录：
来源帧、来源相机、是否 GT、生成段、上游来源链、
几何一致性、外观质量、首次写入时间、最后验证时间
```

建立三层状态：

- **GT 锚定的可见外观**：高优先级保存；其预测几何仍允许修正。
- **稳定生成区域**：多次回访仍能对齐的生成内容，作为较强记忆。
- **候选区域**：刚生成、证据不足或存在冲突的内容，先弱约束使用。

先做不训练的门控：新区域允许添加；已有可靠区域出现冲突时，不立即覆盖；连续验证通过再提升优先级；失败则回退候选更新。没有被看到不等于已被删除。

可借用本地 [GaME](https://github.com/VladimirYugay/GaME) 的新增、冲突与删除处理代码，但“识别世界发生了变化”和“识别生成器画错了”是不同问题。这里提出的来源分层和写回门控需要新加，不是 GaME 开箱即有。

**最小实验：** 在离线来源帧中人为注入一块颜色漂移或结构错位，比较无门控、GT 全局加权、分区域门控三种更新策略；再检查后续渲染是否扩散该错误，以及真实新区域能否正常进入缓存。

**关键研究点：** 同一段生成视频的 5 张相似图不是 5 份独立证据；门控应考虑共同的生成来源，避免“同一个错误出现很多次，就被当成非常可信”。多轮自洽只能证明内部稳定，不能证明隐藏世界真实。

接入：[来源与权重组织](scripts/worldwarp_map_geometry.py)；[GaME 地图状态与更新](GaME/src/entities/game.py)。

### 课题 5：持久 GS 的局部增量融合与预算管理

**核心问题：下一段能否沿用旧 GS，只把新看到的区域写进去？**

目标从当前的：

```text
本轮来源图 → 从零拟合 Gₖ
```

变成：

```text
旧地图 Gₖ₋₁ ＋ 新来源几何 ΔP / ΔG
    → 统一坐标
    → 重复区域匹配与冲突判断
    → 更新旧高斯 / 增加新高斯
    → 少量局部优化
    → 保存 Gₖ
```

建议先限制为一个房间、静态场景、短轨迹，只处理新增与局部修正，不一开始就做无限世界。

最小实施顺序：

1. 给旧高斯持久 ID，跨轮恢复参数；需要续优化时同时维护匹配的优化器状态。
2. 在可靠共视区域对齐新几何；已有全局坐标并且一致时，不再重复估计无意义的变换。
3. 用位置、表面朝向、深度与外观共同判断重复，避免把前后两个表面只因距离近而合并。
4. 只优化新增高斯和邻近受影响区域；保留一小组旧视图重放，检测遗忘。
5. 设置活动地图预算，对远离当前视角的子图保存到 CPU / 磁盘，返回时重载。

首选复用本地 GaME；[MonoGS](https://github.com/muskie82/MonoGS) 可作为关键帧与在线 GS 优化的实现参考。另有直接相关的论文 [Learning Efficient Fuse-and-Refine for Feed-Forward 3D Gaussian Splatting](https://papers.neurips.cc/paper_files/paper/2025/hash/6e9d416f7ffeb2042729ddee8d3138a3-Abstract-Conference.html)，研究把前馈高斯在规范三维空间中融合、去冗余和修正；本轮核实了论文，未核实可直接使用的官方代码与权重，不把它列为即插即用组件。

**最小实验：** 在同一组固定历史 RGB-D 上比较“每轮重建”和“恢复旧地图＋局部更新”，固定总优化时间或绘制耗时—质量曲线。然后测试向左探索后回到起点的保持能力。

**研究增量：** 生成来源下的持久地图更新、冲突恢复和局部计算分配。课题 4 研究“能不能写”，本课题研究“怎么融合、存储和更新”，可以分别做消融。

**边界：** 固定显存不等于无限地图无损记忆。场景范围持续扩大时，需要子图、存储或有损压缩，不能仅靠一个点数上限宣称无限探索。

### 课题 6：质量与信息增益驱动的关键帧选择

**核心问题：当前每段均匀选的 5 帧，是否真的是最值得建图的 5 帧？**

保留相同来源数量，只改变选法，是很适合先做的小课题。

候选评分可以包含：

- 对旧缓存未覆盖区域的新增覆盖。
- 与已选视图的射线 / 视角差异，以及有平移时的有效基线。
- 纹理清晰度、几何有效性与重投影一致性。
- 对未来请求视角的可见性贡献。
- 对旧区域回访的价值，而不只保留最近一段。

采用贪心覆盖选择即可起步，同时保留 GT 锚点。后续可用 [CoTracker](https://github.com/facebookresearch/co-tracker) 的点轨迹帮助检测重复、漂移与短时跟踪失败，但点轨迹不是深度真值。

**最小实验：** 均匀选帧、只按清晰度选、质量＋新增覆盖联合选，三组保持相同图像数、点预算、优化次数和视频配置。

不要同时改视频生成的末尾连续 5 帧上下文：那是另一套历史。先仅修改 [select_history](scripts/worldwarp_map_geometry.py)，才能判断几何选帧本身的作用。

**研究增量：** 生成历史中“有价值的新信息”与“好看的重复帧”分开；回访时检索较早的好帧，减少单纯滚动窗口的遗忘。

## 4. 另外六个可以独立切入的课题

### 课题 7：旧地图约束的新几何预测

**核心问题：旧区域明明已有深度，为什么每轮 MapAnything 都要把它重新猜一遍？**

最小版本不必换模型：把旧 GS 在当前来源相机下渲染的深度作为可选几何提示，只在高可信共视区域使用；新增、遮挡边界和冲突区域继续由图像恢复。

MapAnything 本身支持 RGB、相机与深度的组合输入，本地 README 给出了 `depth_z` 与标定的要求；当前 [专用桥接器](scripts/mapanything_worldwarp_rgbd.py) 尚未传入旧深度。可先利用这一现成接口，见 [本地多模态说明](MapAnything/README.md)。

要特别处理三件事：

1. 传入的是 z-depth 还是沿射线距离，必须一致。
2. 旧图无覆盖的位置不能伪装成有效深度，按接口约定处理缺失值和预处理。
3. 旧深度也是模型假设，允许新证据修正，不能为了稳定而锁死错误尺度。

如果关注更长序列，再比较 [TTT3R](https://github.com/Inception3D/TTT3R) 的递归状态更新，或 [DA3-Streaming](https://github.com/ByteDance-Seed/Depth-Anything-3/tree/main/da3_streaming) 的分块几何处理。它们分别提供状态更新 / 分块长序列的参考；不等同于已经接好的持久 GS 地图。

**最小实验：** 无旧深度提示、全覆盖旧深度提示、可信区域稀疏提示；比较跨段深度稳定性和带平移的渲染。再人为给旧深度加偏差，检查系统能否纠错，避免只有“更稳定但更错”的结果。

**研究增量：** 可信几何先验的选择性继承，以及旧先验失效时的退让机制。完整替换成流式网络是第二阶段，不是这个小课题的前置条件。

### 课题 8：请求相机与生成画面的轻量校准

**核心问题：如果视频没有完全按请求相机运动，强行固定该位姿拟合 GS，会不会把相机误差变成几何变形？**

此处所指 MapAnything 桥接分支的来源相机来自请求轨迹，GS 拟合时 `optimize_poses=False`；原版后续重建还使用 TTT3R 历史相对位姿，不能套用同一描述。可以引入两套变量：

- `C_request`：用户想要的相机，是生成控制目标。
- `C_estimated`：解释当前生成图像所需的相机估计，用于诊断和重建校准。

最小版本只估计小量旋转偏差；带平移时才加入受约束的平移与尺度修正。固定第一相机作为坐标锚点，在可靠重叠区域用匹配 / 点图对齐，并加入对请求位姿的正则约束。

可参考 [MASt3R-SLAM](https://github.com/rmurai0610/MASt3R-SLAM) 的几何先验与跟踪优化，也可以先使用 MapAnything 已保存的预测相机作诊断。需要独立判断时，不应把“已经以请求相机为条件的预测”当成完全独立的证据。

**最小实验：** 固定位姿、仅修旋转、小范围修位姿三组；同时报告重投影误差与相对请求轨迹的偏差。渲染更贴图但镜头越来越不听指令，也不能算成功。

**研究增量：** 在“忠于控制”与“解释生成观测”之间进行受限校准，而不是把控制轨迹直接换成估计轨迹。

纯旋转没有三角化基线，不能通过这类对齐凭空恢复真实尺度。对同中心相机中心直接做 Sim(3) 拟合也是退化的，应改用可靠射线、旋转或有支持的几何对齐。

### 课题 9：遮挡与可靠性感知的噪声调度

**核心问题：可靠内容少改，可疑内容多修，没见过的内容充分生成，能不能由模块自动决定？**

当前链路是 `Alpha×来源支持 → 0.5 阈值 → 15×15 腐蚀 → 潜空间布尔掩码`，最后仅区分两套噪声起点。可以把几个概念分开：

- **覆盖**：有没有来源和渲染支持。
- **几何可靠性**：投影、深度、遮挡是否一致。
- **外观可靠性**：是否清楚、是否来自 GT、是否经过很多轮生成。
- **边界风险**：深度断层、空洞和来源切换附近是否容易出错。

先形成高 / 中 / 低三级，再尝试连续可信度 `q`。一种待验证的策略是：

```text
可信旧区域      → 较低起始噪声，保留更多条件
有覆盖但有冲突  → 中等起始噪声，允许修正
无可靠支持      → 较高起始噪声，允许补全
历史上下文      → 仍独立处理，不与上述等级混用
```

基础直接用 [WorldWarp](https://github.com/HyoKong/WorldWarp) 现有空间变化时间步机制，以及 MapAnything 保存的 confidence 和几何重投影结果，不一定先训练一个新模型。

**实现重点：** 不能只把 `valid_alpha.npy` 改成浮点分数。当前 [sample_sequence](WorldWarp/pose_control.py) 会转成 `.bool()`；必须同时修改掩码预处理、起始 sigma、逐位置时间表和传给 Transformer 的 timestep，保证使用同一条调度定义。confidence 先做标定或分位数分析，不直接当成正确概率。

掩码部分也能单独形成更小实验：固定二值调度，只比较固定 15×15 腐蚀与深度边界感知腐蚀；再比较简单时间抽样与考虑 VAE 时间邻域的有效性聚合。不要一次把两项都换掉。

**验证：** 已知区域的保持、错误条件的可修复性、新区域质量、边界闪烁四项一起看。研究增量是可信度如何决定“保留多少”，不是简单把掩码做得更大或把噪声全部降低。

### 课题 10：低损耗上下文与潜空间记忆

**核心问题：除了几何变差，重复解码、压视频、读视频、再编码是否也在损伤历史？**

建议先分成三个难度逐渐增加的版本：

1. **无损 RGB 历史**：几何来源和上下文直接使用解码后的张量 / 无损图像，MP4 只用于观看，避免从有损成片反复取监督图。
2. **同一处理网格下的上下文缓存**：减少不必要的 resize、裁切和色彩格式往返。
3. **潜空间上下文复用**：在兼容的窗口、时间位置和 VAE 规则下复用历史 latent，而不是每段重新编码同一段 RGB。

第三项不是把任意片段 latent 直接拼起来即可：视频 VAE 有时间感受野、首帧特殊处理与分块边界，单独编码和整段编码可能不同。要先做编解码一致性测试，再进入去噪实验。

可直接复用本地 WorldWarp / Wan VAE 和采样代码，不必先增加一个大型开源项目。课题 3 保存可重投影的图像外观；本课题控制图像 / 潜变量在模块间往返的损耗，两者互补。

**最小实验：** 分别测“来源→GS 渲染”“同一图像→VAE 往返”“无损历史与 MP4 历史”“最终闭环”的损失。没有实际 profile 前，不预设视频编码就是主要问题。

**研究增量：** 跨段上下文缓存的一致性与低损耗传递。若进一步加入长期参考 attention 或 reference adapter，需要训练与兼容性工作，应视作扩展课题。

### 课题 11：事件驱动的缓存更新与计算分配

**核心问题：所有视频段都值得重新跑几何和优化 500 步吗？**

保留现有视频段长不动，只在段边界决定：

- 新增覆盖很少、旧条件稳定：沿用上次 GS。
- 有少量新区域：局部增补和短优化。
- 出现显著视差、回访冲突或可信度下降：进行较完整的重建 / 校准。

触发信号可来自预计目标视角的覆盖、相机运动、历史图与旧地图渲染的残差。判断时只能用当时已经获得的历史和已知请求相机，不能提前看尚未生成的未来 RGB。

**最小实验：** 同样的 4 段设置，对比每段更新、隔段更新、事件触发更新；报告总时间和质量，画 Pareto 曲线。工程优化再单独测常驻模型、减少文件交换的收益，避免与算法收益混算。

接入现有 [MapAnything 分支调度](scripts/generate_worldwarp_mapanything.py) 即可设计原型。最初“复用上一份完整缓存”不必等待课题 5 的局部融合全部实现。

**研究增量：** 把计算预算分配到真正缺信息的段和区域。改变视频生成段长是更大改动，会触及 `4k+1` 帧约束、上下文重叠和当前四段脚本，建议后置。

### 课题 12：静态背景与动态对象分层缓存

**核心问题：以后画面里有人或物体运动，怎样避免把运动轨迹融合成静态拖影？**

从一个很小的动态问题起步：单个运动对象＋固定背景，不直接追求完整 4D 世界模型。

```text
RGB 历史
  → 对象分割与跟踪
  ├→ 静态区域：更新持久背景 GS
  └→ 动态区域：独立对象状态 / 时间条件高斯
                         ↓
              指定相机、指定时刻合成渲染
```

第一阶段只研究“不污染背景”：把动态掩码从静态建图监督里剔除。可用 [SAM 2](https://github.com/facebookresearch/sam2) 跟踪对象掩码，配合 CoTracker；语义上的“人 / 椅子”不等于实际上运动，仍需运动证据。

第二阶段再参考 [4D Gaussian Splatting](https://github.com/hustvl/4DGaussians) 的时间形变表示。较新的 [StreamSplat](https://github.com/DSL-Lab/StreamSplat) 也值得读，但其公开推理脚本接收 RGB 帧和预计算深度，示例输出渲染视频；接本管线仍需核对相机 / 投影模型、GS 导出和状态接口。不能仅凭项目介绍中的“在线”就视作可替换整个持久建图后端，见 [官方推理脚本](https://github.com/DSL-Lab/StreamSplat/blob/main/splat_inference.py)。

**最小实验：** 先用有真实运动的视频验证静态背景是否更干净，再考虑生成视频。报告对象离开后背景残影和静态区域质量。

**边界：** 只有一张 GT 时，未来运动来自生成假设，不是能重建出来的真实时间规律。这里的动态重建也不自动提供“想象未来运动”的能力。该课题相关，但不是当前静态教室流程的优先项。

## 5. 前馈高斯到底替换到哪一层：接入时必须统一的接口

为了让课题 1 的不同项目可比较，建议先建立一个薄适配层，而不是改动 WorldWarp 的核心生成网络。

```text
输入：来源 RGB、可选深度、K、来源相机、来源记录
              ↓
       可替换的几何 / GS 后端
              ↓
输出：目标视角 RGB、Alpha、深度、可靠性、可保存的场景状态
              ↓
       现有 WorldWarp 条件接口
```

前馈 GS 可以替换 B～C 整段；如果只研究更好的高斯参数头，也可以保留 MapAnything，只替换 C。后者通常需要设计或训练适配头，不能假设某个独立 RGB→GS 网络直接接受 MapAnything 的点图。

建议统一以下约定：

| 接口内容 | 必须约定清楚的事 |
|---|---|
| 相机 | c2w / w2c、轴方向、像素单位 K、裁剪与缩放后的 K |
| 尺度 | 全局单位、单图尺度先验、每轮是否重新归一化 |
| 高斯参数 | 坐标、尺度 / log-scale、四元数顺序、不透明度 / logit、SH 或纹理格式 |
| 几何输出 | z-depth 与射线距离的区别，以及无效深度表示 |
| 渲染输出 | RGB 范围、色彩空间、Alpha 和“可信度”分别输出 |
| 来源记录 | GT / generated、来源帧号、来源链、质量与可见范围 |
| 状态恢复 | 参数、全局变换、关键帧、来源记录；训练续跑另加优化器等状态 |

对于规范坐标系输出的前馈模型，若用相似变换 `X′=sRX+t` 对齐到全局，高斯协方差也要按 `Σ′=s²RΣRᵀ` 变换；使用世界方向定义的高阶 SH 时也要处理旋转。只移动高斯中心而不处理形状 / 方向颜色，可能导致看似对齐却渲染不一致。

一个模型即使能导出 PLY，也不代表有完整的继续训练、来源管理与地图恢复接口。应分别验证“能渲染”和“能增量更新”。

## 6. 怎么验证这些课题，而不是只得到几个更好看的视频

### 6.1 先用现有产物做模块回放，再做真正闭环

本地已经有可直接用于离线测试的 [anchor_gs 产物](WorldWarp_outputs/classroom_worldwarp_mapanything_2026-09-08/anchor_gs/pipeline_report.json)：

- `source_*.png`：实际来源图。
- `posed_rgbd.npz`：深度、相机、confidence、GT 标记。
- `native_3dgs.pt`：当前基线 GS。
- `warped_rgb.npy`、`valid_alpha.npy`：生成条件。
- `target_cameras.npz`：目标视角。

对 GS、融合、选帧、纹理等课题，先固定这些来源做可控回放；选出有效方案后，再让它使用自己新生成的历史运行闭环。离线胜出不保证闭环胜出，两阶段都需要。

课题 4、5 可先在离线序列上模拟“逐轮写入→渲染→回访”；课题 9、10 才需要较早进入视频采样器。这样可以降低试错成本。

### 6.2 三类轨迹，回答不同问题

| 轨迹 | 最适合验证 | 不该据此声称什么 |
|---|---|---|
| 同中心纯旋转 | 外观保真、来源覆盖、边界与接缝 | 不能独立证明预测深度更准确 |
| 有平移、明显前后景视差 | 深度、遮挡、相机校准、融合 | 单看视觉好看不能证明真实几何 |
| 探索后返回旧视角 | 地图记忆、漂移、覆盖错误与遗忘 | 新区域内部一致不等于真实世界真值 |

当前教室实验已经能支撑第一类的开发。第二类应补充有已知相机 / 深度的渲染场景或真实多视图数据；若主任务仍限定单图，额外真值只能用于评测，不能泄漏为方法输入。

至少覆盖几个不同场景，并使用多个种子复核，不用单一教室、单一种子做总体结论。

### 6.3 指标分组，不把“更平滑”当成“更好”

- **GT 已知区域**：重投影后 PSNR、SSIM、[LPIPS](https://github.com/richzhang/PerceptualSimilarity)，加文字 / 细结构局部检查。
- **新生成区域**：几何重投影一致性、边界、结构稳定性和人工比较；没有 GT 就不计算所谓真值 PSNR。
- **长期记忆**：回访误差、来源保留、错误写回率、旧区域退化随段数的曲线。
- **几何质量**：有真值时评估深度与几何；没有真值时明确称为一致性指标。
- **效率**：几何推理、GS 初始化 / 优化、渲染、视频去噪分别计时；分别统计进程和整体显存峰值、GS 数量、CPU / 磁盘占用。

当前实验已观察到较低时间误差与后半段变软同时存在，不能单用时间 MAE 排名。见 [现有对照与限制](WorldWarp_outputs/classroom_worldwarp_mapanything_2026-09-08/README.md)。

最好额外保存四个阶段的对照：来源图、GS 原始渲染、VAE 往返、最终生成。这样能知道收益究竟发生在几何缓存，还是被后续 VAE / 去噪抹掉。

### 6.4 什么程度算一个可交付的小课题

每个课题至少交付：

1. 一个独立开关或可替换模块。
2. 一个明确的原始基线，以及同预算的必要对照。
3. 模块级与闭环级结果，成功案例和失败案例都保留。
4. 清楚说明“改善了哪个指标、在什么场景成立”。

“接上一个开源模型”是有用工程，但不自动是新的研究贡献。更有价值的问题是：现成方法在 **GT＋自生成历史＋反复回写** 这个条件下为什么失效，你针对这个失效机制做了什么。

## 7. 我建议实际怎样排顺序

### 第一条线：先把当前画面变清楚、变稳定

建议顺序：**损失分段诊断 → 课题 3 外观双缓存 → 课题 9 可靠性调度 → 课题 6 选帧 / 课题 2 初始化**。

理由：这些能直接利用现有教室案例和缓存文件，多数不需要训练，也不需要先接完一个持久 SLAM 系统。

课题 10 的无损历史版本可以作为共同基础，但要实测收益；潜空间缓存放在普通 RGB 路径确认后。

### 第二条线：形成独立的快速几何缓存课题

建议顺序：**统一接口 → SHARP 首轮 / DA3 或 AnySplat 后续基线 → 课题 1 少量可信优化 → 课题 11 更新预算**。

最自然的课题表述是：**面向生成式空间探索的快速、可信高斯缓存构建**。重点不是每秒能预测多少 GS，而是在相同端到端预算下，是否更少遗忘、更少错误传播。

先选一两个模型，避免同时安装所有前馈项目后陷入接口比较。权重与代码许可证分开核对：例如 SHARP / LGTM 有独立模型许可，DA3 的许可随型号而异，FreeSplatter 也有附加条件；本文所称“可用项目”是公开代码研究基础，不是统一的商用授权结论。

### 第三条线：把短视频闭环推进到持久世界

建议顺序：**课题 4 写回门控 → 课题 5 持久融合 → 课题 7 几何继承 / 课题 8 位姿校准 → 回访验证**。

先保证旧内容不会被随意覆盖，再追求地图越建越大。前馈高斯可以提供新区域提案，但可信写回和持久融合仍是独立模块。

本地 GaME 在接通这条线前还有一个具体检查点：`is_keyframe` 中 `as_euler("xyz")` 的默认结果为弧度，却与 50 比较。对当前小角度纯旋转，这条旋转触发条件不会正常发挥作用，应先做单位与阈值测试。这属于接线前的实现校准，不应包装成算法贡献。见 [本地方法](GaME/src/entities/game.py)。

课题 12 动态分层放在静态回访稳定之后。它很相关，但会同时引入对象跟踪、时间状态、遮挡和运动先验，不适合作为目前第一个“小课题”。

## 2026-09-14 历史建议（不作为最新优先级）

如果现在只选三个，我会选：

1. **几何—外观双缓存**：解决“首图信息如何长期保住”。
2. **来源感知的可信写回＋可靠性调度**：分别从写入端和读取端控制生成错误的传播。
3. **前馈高斯＋局部增量修正**：解决“缓存怎样低成本持续更新”。

这三个方向分别对应 **记忆的内容、记忆的可信度、记忆的更新成本**，能独立研究，也能最后合成同一条类 Atlas 管线。

2026-09-14 初稿仅新增本研究文档；原流程 Markdown、架构 SVG、PPT 和项目实现均未修改，也未安装新模型或启动生成实验。2026-09-19 在本文顶部追加研究复审并澄清分支适用范围；本次同样仅更新文档及工作台账，没有启动新实验。
